#!/usr/bin/env python

try:
    from zk_shell.cli import CLI
except ImportError:
    # running from source?
    import sys
    sys.path.extend((".", ".."))
    from zk_shell.cli import CLI


if __name__ == "__main__":
    CLI()()
